---
name: aura-deployen
description: "/aura-deployen — /aura deploy: Abkürzung auf Deutsch. Vercel·Docker·Deployment-Konfiguration."
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, WebFetch
---

Abkürzung `/aura-deployen` auf Deutsch, entspricht `/aura deploy:`.

Sofort `skill: "aura"` aufrufen mit `args: "deploy:$ARGUMENTS"`.
