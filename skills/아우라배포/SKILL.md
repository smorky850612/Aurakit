---
name: 아우라배포
description: "/아우라배포 — /aura deploy: 한글 단축어. Vercel·Docker·배포 설정."
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, WebFetch
---

이것은 `/아우라배포` 한글 단축어입니다. `/aura deploy:$ARGUMENTS`와 동일합니다.

Skill 도구로 `skill: "aura"`, `args: "deploy:$ARGUMENTS"`를 즉시 호출한다.
