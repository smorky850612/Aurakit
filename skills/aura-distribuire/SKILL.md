---
name: aura-distribuire
description: "/aura-distribuire — /aura deploy: scorciatoia italiana. Vercel·Docker·configurazione deployment."
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, WebFetch
---

Scorciatoia `/aura-distribuire` in italiano, equivalente a `/aura deploy:`.

Invoca immediatamente `skill: "aura"` con `args: "deploy:$ARGUMENTS"`.
