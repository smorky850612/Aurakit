---
name: aura-construire
description: "/aura-construire — /aura build: raccourci français. Implémenter, créer, générer des fonctions."
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, WebFetch
---

Raccourci `/aura-construire` en français, équivalent à `/aura build:`.

Invoquer immédiatement `skill: "aura"` avec `args: "build:$ARGUMENTS"`.
