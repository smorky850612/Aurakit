---
name: 아우라
description: "/aura 한글 단축어 — /아우라로 /aura 실행. 한/영 전환 없이 사용 가능."
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, WebFetch
---

이것은 `/아우라` 한글 단축어입니다. `/aura`와 동일하게 동작합니다.

`$ARGUMENTS`를 그대로 유지하며 `/aura` 스킬의 전체 동작을 수행한다.
Skill 도구로 `skill: "aura"`, `args: "$ARGUMENTS"`를 즉시 호출한다.
