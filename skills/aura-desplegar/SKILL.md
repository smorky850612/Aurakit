---
name: aura-desplegar
description: "/aura-desplegar — /aura deploy: atajo en español. Vercel·Docker·configuración de despliegue."
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, WebFetch
---

Este es el atajo `/aura-desplegar` en español, equivalente a `/aura deploy:`.

Invoca inmediatamente `skill: "aura"` con `args: "deploy:$ARGUMENTS"`.
