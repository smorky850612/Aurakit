---
name: 奥拉构建
description: "/奥拉构建 — /aura build: 中文快捷方式。功能实现、添加、生成。"
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, WebFetch
---

这是 `/奥拉构建` 中文快捷方式，等同于 `/aura build:`。

立即调用 `skill: "aura"`，参数 `args: "build:$ARGUMENTS"`。
