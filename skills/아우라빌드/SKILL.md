---
name: 아우라빌드
description: "/아우라빌드 — /aura build: 한글 단축어. 기능 구현·추가·생성."
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, WebFetch
---

이것은 `/아우라빌드` 한글 단축어입니다. `/aura build:$ARGUMENTS`와 동일합니다.

Skill 도구로 `skill: "aura"`, `args: "build:$ARGUMENTS"`를 즉시 호출한다.
