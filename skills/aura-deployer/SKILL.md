---
name: aura-deployer
description: "/aura-deployer — /aura deploy: raccourci français. Vercel·Docker·configuration de déploiement."
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, WebFetch
---

Raccourci `/aura-deployer` en français, équivalent à `/aura deploy:`.

Invoquer immédiatement `skill: "aura"` avec `args: "deploy:$ARGUMENTS"`.
