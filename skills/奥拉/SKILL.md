---
name: 奥拉
description: "/奥拉 — /aura 中文快捷方式。无需切换输入法，直接使用 /aura。"
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, WebFetch
---

这是 `/奥拉` 中文快捷方式，等同于 `/aura`。

立即调用 `skill: "aura"`，参数 `args: "$ARGUMENTS"`。
