---
name: オーラデプロイ
description: "/オーラデプロイ — /aura deploy: 日本語ショートカット。Vercel・Docker・デプロイ設定。"
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, WebFetch
---

これは `/オーラデプロイ` 日本語ショートカットです。`/aura deploy:$ARGUMENTS` と同じです。

Skill ツールで `skill: "aura"`, `args: "deploy:$ARGUMENTS"` を即時呼び出す。
